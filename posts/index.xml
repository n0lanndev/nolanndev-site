<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>The Lab on nolanndev | C&#43;&#43; &amp; DevSecOps</title>
        <link>https://nolanndev.fr/posts/</link>
        <description>Recent content in The Lab on nolanndev | C&#43;&#43; &amp; DevSecOps</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>fr-fr</language>
        <lastBuildDate>Tue, 12 May 2026 00:00:00 +0000</lastBuildDate>
        <atom:link href="https://nolanndev.fr/posts/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>Construire un Packer &amp; Loader PE de A à Z</title>
            <link>https://nolanndev.fr/posts/packer/create-packer/</link>
            <pubDate>Tue, 12 May 2026 00:00:00 +0000</pubDate>
            
            <guid>https://nolanndev.fr/posts/packer/create-packer/</guid>
            <description>&lt;p&gt;Bienvenue dans ce guide. Nous allons apprendre à créer un Packer, un outil capable de chiffrer un programme Windows (.exe) et de le charger directement en mémoire sans qu&amp;rsquo;il ne touche jamais le disque.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Note : Ce contenu est à but purement éducatif. Voir l&amp;rsquo;avertissement complet en &lt;a href=&#34;#disclaimer&#34;&gt;fin d&amp;rsquo;article&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;theorie&#34;&gt;Théorie : Comment Windows lance-t-il un programme ?&lt;/h2&gt;
&lt;p&gt;Pour construire un loader, il faut d&amp;rsquo;abord comprendre ce qu&amp;rsquo;est réellement un fichier .exe. Sous Windows, on utilise le format PE (&lt;em&gt;Portable Executable&lt;/em&gt;).&lt;/p&gt;</description>
            <content type="html"><![CDATA[<p>Bienvenue dans ce guide. Nous allons apprendre à créer un Packer, un outil capable de chiffrer un programme Windows (.exe) et de le charger directement en mémoire sans qu&rsquo;il ne touche jamais le disque.</p>
<blockquote>
<p>Note : Ce contenu est à but purement éducatif. Voir l&rsquo;avertissement complet en <a href="#disclaimer">fin d&rsquo;article</a>.</p>
</blockquote>
<h2 id="theorie">Théorie : Comment Windows lance-t-il un programme ?</h2>
<p>Pour construire un loader, il faut d&rsquo;abord comprendre ce qu&rsquo;est réellement un fichier .exe. Sous Windows, on utilise le format PE (<em>Portable Executable</em>).</p>
<p>Un fichier PE n&rsquo;est pas simplement une suite d&rsquo;instructions machine : c&rsquo;est une structure normalisée contenant toutes les informations nécessaires au système pour charger le programme en mémoire, résoudre ses dépendances, appliquer ses protections mémoire et enfin exécuter son point d&rsquo;entrée.</p>
<p>Pour nous guider, nous allons utiliser la carte de référence absolue : le Dissected PE :</p>

<figure>
    <img src="/img/packer/pefile.png"
          alt="Anatomie d&#39;un fichier PE" 
         class="left"
          />

    
        <figcaption>
            Figure 1 : Anatomie d&rsquo;un fichier PE<br>
Source : <a href="https://onlyf8.com/pe-format">https://onlyf8.com/pe-format</a>
        </figcaption>
    
</figure>

<h3 id="vue-densemble--ce-que-fait-windows-quand-on-double-clique-sur-un-exe">Vue d&rsquo;ensemble : ce que fait Windows quand on double-clique sur un .exe</h3>
<p>Lorsqu&rsquo;un utilisateur lance un programme, Windows ne &ldquo;lit pas juste le fichier puis exécute le code&rdquo;. Il suit en réalité plusieurs étapes précises :</p>
<ol>
<li>Ouvre le fichier sur disque</li>
<li>Vérifie qu&rsquo;il s&rsquo;agit bien d&rsquo;un exécutable PE valide</li>
<li>Réserve un espace mémoire pour l&rsquo;image du programme</li>
<li>Copie les différentes sections (.text, .data, .rdata, etc.) en mémoire</li>
<li>Charge les DLL nécessaires (kernel32.dll, user32.dll, &hellip;)</li>
<li>Résout les imports (CreateFileA, MessageBoxA, etc.)</li>
<li>Applique les relocations si l&rsquo;image n&rsquo;est pas chargée à son adresse préférée</li>
<li>Configure les protections mémoire (RX, RW, etc.)</li>
<li>Lance le point d&rsquo;entrée (EntryPoint)</li>
</ol>
<h3 id="anatomie-dun-fichier-pe">Anatomie d&rsquo;un fichier PE</h3>
<p>L&rsquo;image montre qu&rsquo;un exécutable Windows est divisé en plusieurs blocs logiques :</p>
<ul>
<li><strong>Headers</strong> : métadonnées décrivant le programme</li>
<li><strong>Sections</strong> : contenu réel (code, données, ressources&hellip;)</li>
<li><strong>Imports</strong> : fonctions externes utilisées</li>
<li><strong>Relocations</strong> : ajustements d&rsquo;adresses mémoire</li>
<li><strong>Entry Point</strong> : première instruction exécutée</li>
</ul>
<p>Voyons cela proprement.</p>
<h3 id="1-le-dos-header-">1. Le DOS Header :</h3>
<p>Tout fichier PE commence par deux octets célèbres :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">4</span>D <span style="color:#ae81ff">5</span>A
</span></span></code></pre></div><p>Ce sont les caractères :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>MZ
</span></span></code></pre></div><p>Signature historique des exécutables MS-DOS.</p>
<p>Le champ le plus important ici est :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>e_lfanew
</span></span></code></pre></div><p>Il contient l&rsquo;offset du vrai header PE dans le fichier.</p>
<p>En clair :</p>
<blockquote>
<p>Windows lit d&rsquo;abord le header DOS, puis saute à e_lfanew pour trouver la structure moderne.</p>
</blockquote>
<h3 id="2-le-pe-header--identité-du-programme">2. Le PE Header : identité du programme</h3>
<p>À l&rsquo;offset indiqué par e_lfanew, on trouve :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">50</span> <span style="color:#ae81ff">45</span> <span style="color:#ae81ff">00</span> <span style="color:#ae81ff">00</span>
</span></span></code></pre></div><p>Soit :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>PE<span style="color:#960050;background-color:#1e0010">\</span><span style="color:#ae81ff">0</span><span style="color:#960050;background-color:#1e0010">\</span><span style="color:#ae81ff">0</span>
</span></span></code></pre></div><p>C&rsquo;est la signature officielle du format Portable Executable.</p>
<p>Ensuite viennent plusieurs champs importants :</p>
<h4 id="machine">Machine</h4>
<p>Malgré son nom, ce champ ne désigne pas &ldquo;l’ordinateur&rdquo;, mais
l’architecture processeur visée par le binaire.</p>
<ul>
<li><strong>0x14C</strong>   -&gt; x86 (32 bits)</li>
<li><strong>0x8664</strong>  -&gt; x64</li>
<li><strong>0xAA64</strong>  -&gt; ARM64</li>
</ul>
<h4 id="numberofsections">NumberOfSections</h4>
<p>Nombre de sections présentes (.text, .data, etc.)</p>
<h4 id="sizeofoptionalheader">SizeOfOptionalHeader</h4>
<p>Taille, en octets, du header suivant :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_OPTIONAL_HEADER
</span></span></code></pre></div><p>Ce champ permet à Windows de savoir où commence la table des sections juste après.</p>
<p>Valeurs classiques :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">0xE0</span> <span style="color:#f92672">-&gt;</span> PE32
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">0xF0</span> <span style="color:#f92672">-&gt;</span> PE32<span style="color:#f92672">+</span> (x64)
</span></span></code></pre></div><h4 id="characteristics">Characteristics</h4>
<p>Champ de flags décrivant la nature du fichier.</p>
<p>Exemples :</p>
<ul>
<li>exécutable (<em>EXE</em>)</li>
<li>bibliothèque dynamique (<em>DLL</em>)</li>
<li>32 bits</li>
<li>gros fichier sans symboles</li>
<li>relocs supprimées</li>
<li>etc.</li>
</ul>
<p>C’est en quelque sorte la carte d’identité du binaire.</p>
<h3 id="3-optional-header-qui-nest-pas-optionnel">3. Optional Header (qui n&rsquo;est pas optionnel)</h3>
<p>Malgré son nom, cette structure est indispensable.</p>
<p>Elle contient les paramètres de chargement les plus importants :</p>
<h4 id="imagebase">ImageBase</h4>
<p>Adresse mémoire préférée du programme :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">0x140000000</span>
</span></span></code></pre></div><h4 id="addressofentrypoint">AddressOfEntryPoint</h4>
<p>Offset de la première instruction à exécuter.</p>
<h4 id="sizeofimage">SizeOfImage</h4>
<p>Taille totale occupée en mémoire après chargement.</p>
<h4 id="sectionalignment--filealignment">SectionAlignment / FileAlignment</h4>
<p>Règles d&rsquo;alignement :</p>
<ul>
<li>Sur disque</li>
<li>En mémoire</li>
</ul>
<p>Très important lors d&rsquo;un packer ou d&rsquo;une reconstruction manuelle.</p>
<h3 id="4-les-data-directories">4. Les Data Directories</h3>
<ul>
<li><strong>Import Table</strong> <em>(La liste de courses)</em> : Contient les noms des fonctions externes et des DLL dont le programme a besoin pour fonctionner (ex: CreateFileA dans kernel32.dll).</li>
<li><strong>Export Table</strong> <em>(Le menu des services)</em> : Utilisée principalement par les DLL pour lister les fonctions qu&rsquo;elles mettent à disposition des autres programmes.</li>
<li><strong>Relocation Table</strong> <em>(Le plan de secours)</em> : Liste les endroits du code où Windows doit ajuster les adresses mémoires si le programme n&rsquo;est pas chargé à sa place préférée (indispensable pour l&rsquo;ASLR).</li>
<li><strong>TLS - Thread Local Storage</strong> <em>(Le casier personnel)</em> : Zone de mémoire réservée pour des variables qui sont propres à chaque &ldquo;Thread&rdquo; (fil d&rsquo;exécution) du programme.</li>
<li><strong>Resources</strong> <em>(Le coffre à jouets)</em> : Contient tout ce qui n&rsquo;est pas du code : les icônes du programme, les images, les menus, les boîtes de dialogue ou même des fichiers embarqués.</li>
<li><strong>Exception Table</strong> <em>(Le filet de sécurité)</em> : Contient une liste de tables de gestion d&rsquo;erreurs. Elle permet au système de savoir quoi faire si le programme plante (essentiel sur les architectures 64 bits).</li>
<li><strong>Debug Directory</strong> <em>(Le carnet de notes)</em> : Contient des informations sur la manière dont le programme a été compilé et où se trouvent les fichiers de symboles (.pdb) pour le débogage.</li>
<li><strong>Load Config Directory</strong> <em>(Les consignes de sécurité)</em> : Contient des paramètres de sécurité avancés, comme la configuration du Control Flow Guard (CFG) qui protège contre l&rsquo;exploitation de failles.</li>
<li><strong>IAT - Import Address Table</strong> <em>(Le tableau de branchement)</em> : C&rsquo;est la table réelle où Windows écrit les adresses mémoires finales des fonctions importées une fois que le programme est lancé.</li>
</ul>
<h3 id="les-sections--le-vrai-contenu-du-programme">Les sections : le vrai contenu du programme</h3>
<p>Après les headers viennent les sections.</p>
<p>Chaque section possède :</p>
<ul>
<li>un nom</li>
<li>une adresse virtuelle</li>
<li>une taille</li>
<li>des permissions mémoire</li>
</ul>
<p>Les plus courantes :</p>
<h4 id="text">.text</h4>
<p>Contient le code machine du programme.</p>
<p>Permissions :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>READ <span style="color:#f92672">+</span> EXECUTE
</span></span></code></pre></div><p>C&rsquo;est ici que se trouvent les instructions CPU.</p>
<h4 id="data">.data</h4>
<p>Variables globales modifiables :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">int</span> compteur <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>;
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">char</span> flag <span style="color:#f92672">=</span> <span style="color:#ae81ff">1</span>;
</span></span></code></pre></div><p>Permissions :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>READ <span style="color:#f92672">+</span> WRITE
</span></span></code></pre></div><h4 id="rdata">.rdata</h4>
<p>Données constantes :</p>
<ul>
<li>chaînes statiques</li>
<li>tables virtuelles C++</li>
<li>imports parfois</li>
</ul>
<p>Permissions :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>READ ONLY
</span></span></code></pre></div><h4 id="rsrc">.rsrc</h4>
<p>Icônes, menus, version info, manifestes.</p>
<h4 id="reloc">.reloc</h4>
<p>Table des relocations.</p>
<p>Utilisée si le programme n&rsquo;est pas chargé à ImageBase.</p>
<h3 id="les-imports--parler-avec-windows">Les Imports : parler avec Windows</h3>
<p>Un <strong>.exe</strong> n&rsquo;embarque pas tout Windows.</p>
<p>Quand un programme appelle :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>MessageBoxA(...)
</span></span><span style="display:flex;"><span>CreateFileA(...)
</span></span><span style="display:flex;"><span>VirtualAlloc(...)
</span></span></code></pre></div><p>Ces fonctions viennent de DLL externes :</p>
<ul>
<li>user32.dll</li>
<li>kernel32.dll</li>
<li>ntdll.dll</li>
</ul>
<p>Le fichier PE contient donc une Import Table listant :</p>
<ul>
<li>la DLL à charger</li>
<li>les fonctions nécessaires</li>
</ul>
<p>Le chargeur Windows remplit ensuite l&rsquo;IAT <em>(Import Address Table)</em> avec les vraies adresses mémoire.</p>
<p>Notre futur loader devra refaire cela lui-même.</p>
<h3 id="les-relocations--déplacer-le-programme">Les Relocations : déplacer le programme</h3>
<p>Supposons que le binaire souhaite être chargé ici :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">0x140000000</span>
</span></span></code></pre></div><p>Mais cette zone mémoire est déjà occupée.</p>
<p>Windows le charge ailleurs :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#ae81ff">0x7FF600000000</span>
</span></span></code></pre></div><p>Certaines adresses codées en dur deviennent fausses.
Le loader parcourt alors .reloc et corrige automatiquement les pointeurs.
C&rsquo;est indispensable pour un manual mapping fiable.</p>
<h3 id="le-point-dentrée--naissance-du-processus">Le Point d&rsquo;entrée : naissance du processus</h3>
<p>Quand tout est prêt :</p>
<ul>
<li>sections copiées</li>
<li>imports résolus</li>
<li>relocations appliquées</li>
<li>protections mémoire actives</li>
</ul>
<p>Windows saute vers :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>EntryPoint
</span></span></code></pre></div><p>Sur un programme C/C++, ce n&rsquo;est souvent pas <strong>main()</strong> directement.</p>
<p>On passe généralement par le runtime CRT :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>mainCRTStartup()
</span></span></code></pre></div><p>Qui initialise :</p>
<ul>
<li>heap</li>
<li>variables globales</li>
<li>TLS</li>
<li>constructeurs C++</li>
</ul>
<p>Puis appelle enfin :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>main()
</span></span></code></pre></div><h3 id="pourquoi-tout-cela-nous-intéresse-pour-un-packer-">Pourquoi tout cela nous intéresse pour un packer ?</h3>
<p>Un packer transforme un exécutable en données compressées ou chiffrées.</p>
<p>Ensuite un stub/loader doit :</p>
<ol>
<li>Déchiffrer le payload</li>
<li>Recréer une image PE en mémoire</li>
<li>Résoudre imports et relocations</li>
<li>Exécuter le point d&rsquo;entrée</li>
</ol>
<p>Autrement dit :</p>
<blockquote>
<p>Créer un packer revient à réécrire une version miniature du chargeur de Windows.</p>
</blockquote>
<h2 id="packer">Le Packer : Transformer un .exe en données chiffrées.</h2>
<p>Maintenant que nous comprenons la structure d’un fichier PE, nous pouvons passer à la première étape pratique : transformer un exécutable en payload embarqué.</p>
<p>L’objectif d’un packer est simple :</p>
<ol>
<li>Lire un programme .exe brut</li>
<li>Chiffrer ses octets</li>
<li>Convertir le résultat en tableau C++</li>
<li>Intégrer ce tableau dans un loader</li>
</ol>
<p>Ainsi, le binaire original n’apparaît plus clairement sur disque.</p>
<h3 id="lecture-du-fichier-source">Lecture du fichier source</h3>
<p>Notre packer ouvre l’exécutable en mode binaire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>std<span style="color:#f92672">::</span>ifstream file(filename, std<span style="color:#f92672">::</span>ios<span style="color:#f92672">::</span>binary <span style="color:#f92672">|</span> std<span style="color:#f92672">::</span>ios<span style="color:#f92672">::</span>ate);
</span></span></code></pre></div><ul>
<li><strong>std::ios::binary</strong> : On force la lecture brute. Sans cela, le programme pourrait interpréter certains octets comme des caractères spéciaux (fin de ligne, etc.), ce qui corromprait le .exe.</li>
<li><strong>std::ios::ate</strong> : &ldquo;At The End&rdquo;. On ouvre le fichier et on place le curseur de lecture immédiatement à la fin.</li>
</ul>
<h4 id="récupérer-la-taille-exacte">Récupérer la taille exacte</h4>
<p>Une fois le fichier ouvert à la fin, nous voulons récupérer : &ldquo;À quelle distance du début se trouve-t-on?&rdquo;. C&rsquo;est le rôle de <strong>tellg()</strong> :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>std<span style="color:#f92672">::</span>streamsize size <span style="color:#f92672">=</span> file.tellg();
</span></span></code></pre></div><p>La valeur de size correspond alors précisément au nombre d&rsquo;octets du fichier. Avant de lire, il ne faut pas oublier de rembobiner le curseur au début avec :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>file.seekg(<span style="color:#ae81ff">0</span>, std<span style="color:#f92672">::</span>ios<span style="color:#f92672">::</span>beg);
</span></span></code></pre></div><p>sinon la lecture commencera&hellip; à la fin, et on ne récupérera rien.</p>
<h4 id="le-passage-en-mémoire">Le passage en mémoire</h4>
<p>Une fois qu&rsquo;on connaît la taille du fichier, il faut créer un espace en mémoire (la RAM) pour l&rsquo;accueillir. On utilise pour cela un <strong>std::vector&lt;uint8_t&gt;</strong>.</p>
<p>C&rsquo;est le conteneur idéal pour un packer car :</p>
<ul>
<li>Dynamique : Il s&rsquo;ajuste exactement à la taille <strong>size</strong> de l&rsquo;exécutable.</li>
<li>Brut : Le type <strong>uint8_t</strong> garantit que chaque case fait exactement 1 octet (8 bits). C’est l’unité de mesure universelle pour manipuler des données binaires sans interprétation de texte.</li>
</ul>
<h5 id="le-transfert-des-données">Le transfert des données</h5>
<p>Pour copier le contenu du fichier dans notre vecteur, on utilise la méthode <strong>.read()</strong> :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#75715e">// 1. On réserve l&#39;espace nécessaire dans la RAM
</span></span></span><span style="display:flex;"><span>std<span style="color:#f92672">::</span>vector<span style="color:#f92672">&lt;</span><span style="color:#66d9ef">uint8_t</span><span style="color:#f92672">&gt;</span> buffer(<span style="color:#66d9ef">static_cast</span><span style="color:#f92672">&lt;</span>size_t<span style="color:#f92672">&gt;</span>(size));
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// 2. On recopie le fichier vers cet espace
</span></span></span><span style="display:flex;"><span>file.read(<span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span><span style="color:#66d9ef">char</span> <span style="color:#f92672">*&gt;</span>(buffer.data()), size);
</span></span></code></pre></div><blockquote>
<p>Pourquoi le <strong>reinterpret_cast</strong> ?
La fonction <strong>read</strong> de la bibliothèque standard est un peu ancienne : elle s&rsquo;attend à manipuler des caractères (<strong>char</strong>). Or, nous manipulons des octets bruts (<strong>uint8_t</strong>). On utilise donc <strong>reinterpret_cast</strong> pour dire au compilateur : &ldquo;Ne t&rsquo;inquiète pas, traite ce bloc de mémoire comme s&rsquo;il contenait des caractères pour la lecture, même si ce sont des octets.&rdquo;</p>
</blockquote>
<p>À cette étape précise, l&rsquo;exécutable n&rsquo;est plus seulement un fichier figé sur votre disque dur. Il est devenu un tableau d&rsquo;octets vivant dans la RAM. Chaque octet du programme original est maintenant accessible via buffer[i], prêt à subir une transformation mathématique (comme notre chiffrement XOR).</p>
<h3 id="chiffrement-xor">Chiffrement XOR</h3>
<p>Pour cette démonstration, nous utilisons un XOR simple :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">constexpr</span> <span style="color:#66d9ef">uint8_t</span> XOR_KEY <span style="color:#f92672">=</span> <span style="color:#ae81ff">0x5A</span>;
</span></span></code></pre></div><p>Chaque octet du fichier est transformé :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>byte <span style="color:#f92672">^=</span> XOR_KEY;
</span></span></code></pre></div><p>Le XOR possède une propriété mathématique : il est symétrique. Si vous appliquez la clé une deuxième fois, vous retrouvez la valeur d&rsquo;origine.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>A <span style="color:#f92672">^</span> K <span style="color:#f92672">^</span> K <span style="color:#f92672">=</span> A
</span></span></code></pre></div><p>Cela signifie que le code que nous utilisons pour &ldquo;packer&rdquo; le fichier sera exactement le même que celui utilisé par le &ldquo;loader&rdquo; pour le déchiffrer en mémoire.</p>
<p>Exemple :</p>
<table>
  <thead>
      <tr>
          <th>Octet original</th>
          <th>Clé</th>
          <th>Résultat</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>0x4D</code></td>
          <td><code>0x5A</code></td>
          <td><code>0x17</code></td>
      </tr>
      <tr>
          <td><code>0x17</code></td>
          <td><code>0x5A</code></td>
          <td><code>0x4D</code></td>
      </tr>
      <tr>
          <td><code>0x5A</code></td>
          <td><code>0x5A</code></td>
          <td><code>0x00</code></td>
      </tr>
  </tbody>
</table>
<blockquote>
<p>Note sur la sécurité :
Le XOR avec une clé unique de 1 octet est un chiffrement très faible. Un analyste pourrait retrouver la clé en quelques secondes (via une analyse de fréquence ou en connaissant les premiers octets habituels d&rsquo;un fichier PE).
Ici, l&rsquo;objectif est pédagogique : le but est de comprendre la logique de transformation des données et le fonctionnement d&rsquo;un packer, pas de créer un algorithme inviolable. Dans un outil professionnel, on utiliserait des algorithmes plus complexes comme l&rsquo;AES.</p>
</blockquote>
<h3 id="génération-du-header-c">Génération du header C++</h3>
<p>Une fois chiffré, le payload est exporté sous forme de fichier :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>payload.h
</span></span></code></pre></div><p>Contenant :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#75715e">#pragma once
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;cstdint&gt;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">inline</span> <span style="color:#66d9ef">constexpr</span> std<span style="color:#f92672">::</span>size_t payload_size <span style="color:#f92672">=</span> <span style="color:#ae81ff">69120</span>;
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">inline</span> <span style="color:#66d9ef">constexpr</span> std<span style="color:#f92672">::</span><span style="color:#66d9ef">uint8_t</span> payload[] <span style="color:#f92672">=</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#ae81ff">0x17</span>, <span style="color:#ae81ff">0x00</span>, <span style="color:#ae81ff">0xca</span>, ...
</span></span><span style="display:flex;"><span>};
</span></span></code></pre></div><ul>
<li><strong>constexpr</strong> : Indique au compilateur que la valeur est immuable et déterminée dès la compilation. Pour notre payload, cela signifie que le tableau d&rsquo;octets est gravé directement dans le binaire du loader de façon statique. Le compilateur peut ainsi optimiser l&rsquo;accès à ces données puisqu&rsquo;il sait qu&rsquo;elles ne changeront jamais pendant l&rsquo;exécution.</li>
</ul>
<blockquote>
<p>Différence avec <strong>const</strong>
Le mot-clé <strong>const</strong> signifie que la variable est en lecture seule après avoir été initialisée. Cependant, cette initialisation peut se faire au moment où le programme tourne (<strong>Runtime</strong>).
<strong>Exemple</strong> : Tu demandes à l&rsquo;utilisateur de taper un nombre, et tu le stockes dans un <strong>const int</strong>. La valeur est fixée, mais seulement une fois que l&rsquo;utilisateur a appuyé sur Entrée.</p>
</blockquote>
<ul>
<li><strong>inline</strong> : C&rsquo;est essentiel ici. Comme ce header peut être inclus dans plusieurs fichiers de notre projet, inline évite les erreurs de &ldquo;redéfinition&rdquo; (duplicate symbols) au moment du linkage. Cela permet de définir la variable directement dans le header sans créer de conflit.</li>
</ul>
<blockquote>
<p>Si vous ne comprenez pas bien l&rsquo;importance de inline, je vous recommande vivement cette vidéo qui explique son fonctionnement technique et pourquoi il est indispensable pour éviter les erreurs de linkage : <a href="https://www.youtube.com/watch?v=V7tRfMYemTA">C++ - Le fonctionnement des includes et du mot-clé inline</a></p>
</blockquote>
<p>Le loader pourra inclure directement ce fichier :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&#34;payload.h&#34;</span><span style="color:#75715e">
</span></span></span></code></pre></div><h4 id="résultat-final">Résultat final</h4>
<p>Nous passons donc de par exemple:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>calc.exe
</span></span></code></pre></div><p>à :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>payload.h
</span></span></code></pre></div><p>contenant uniquement des octets chiffrés ainsi que la taille exacte du fichier, prêts à être chargés.</p>
<h2 id="manual-mapping">Le Manual Mapping : Recréer le chargeur de Windows.</h2>
<p>Une fois le payload déchiffré et chargé en mémoire, il ne peut toujours pas être exécuté directement.
Pourquoi ?
Parce qu’un fichier PE sur disque n’est pas utilisable tel quel en mémoire. Il doit être reconstruit exactement comme Windows le ferait :</p>
<ul>
<li>allocation d’une image mémoire complète</li>
<li>copie des headers</li>
<li>copie des sections</li>
<li>préparation pour la résolution des imports</li>
</ul>
<p>C’est exactement le rôle du <strong>manual mapping</strong> : simuler manuellement ce que fait le loader de Windows.</p>
<h3 id="1-charger-et-restaurer-le-pe">1. Charger et restaurer le PE</h3>
<p>On commence par copier le payload en mémoire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">uint8_t</span> <span style="color:#f92672">*</span>localCopy <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#66d9ef">uint8_t</span>[rawSize];
</span></span><span style="display:flex;"><span>std<span style="color:#f92672">::</span>memcpy(localCopy, rawData, rawSize);
</span></span></code></pre></div><p>Puis on déchiffre le binaire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>xorDecryptPayload(localCopy, rawSize, XOR_KEY);
</span></span></code></pre></div><p>À ce stade :</p>
<ul>
<li>on a un PE &ldquo;vivant&rdquo; en RAM</li>
<li>mais encore non validé structurellement</li>
</ul>
<h3 id="2-validation-du-format-pe">2. Validation du format PE</h3>
<p>Avant toute manipulation, on vérifie les signatures classiques du format Portable Executable.</p>
<p><strong>DOS header (MZ)</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_DOS_HEADER <span style="color:#f92672">*</span>dosHeader <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_DOS_HEADER <span style="color:#f92672">*&gt;</span>(localCopy);
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (dosHeader<span style="color:#f92672">-&gt;</span>e_magic <span style="color:#f92672">!=</span> IMAGE_DOS_SIGNATURE)
</span></span></code></pre></div><p><strong>NT header (PE\0\0)</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_NT_HEADERS <span style="color:#f92672">*</span>ntHeaders <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_NT_HEADERS <span style="color:#f92672">*&gt;</span>(localCopy <span style="color:#f92672">+</span> dosHeader<span style="color:#f92672">-&gt;</span>e_lfanew);
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (ntHeaders<span style="color:#f92672">-&gt;</span>Signature <span style="color:#f92672">!=</span> IMAGE_NT_SIGNATURE)
</span></span></code></pre></div><p>Si une de ces signatures est invalide :</p>
<ul>
<li>le fichier n’est pas un PE</li>
<li>on stoppe immédiatement</li>
</ul>
<h3 id="3-allocation-de-limage-en-mémoire">3. Allocation de l’image en mémoire</h3>
<p>On réserve un espace mémoire équivalent à celui que le programme occupera une fois chargé :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>SIZE_T sizeOfImage <span style="color:#f92672">=</span> ntHeaders<span style="color:#f92672">-&gt;</span>OptionalHeader.SizeOfImage;
</span></span><span style="display:flex;"><span>SIZE_T sizeOfHeader <span style="color:#f92672">=</span> ntHeaders<span style="color:#f92672">-&gt;</span>OptionalHeader.SizeOfHeaders;
</span></span></code></pre></div><p>Puis tentative d’allocation à l’adresse préférée du binaire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>BYTE <span style="color:#f92672">*</span>baseAlloc <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>BYTE <span style="color:#f92672">*&gt;</span>(
</span></span><span style="display:flex;"><span>    VirtualAlloc(
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>LPVOID<span style="color:#f92672">&gt;</span>(ntHeaders<span style="color:#f92672">-&gt;</span>OptionalHeader.ImageBase),
</span></span><span style="display:flex;"><span>        sizeOfImage,
</span></span><span style="display:flex;"><span>        MEM_COMMIT <span style="color:#f92672">|</span> MEM_RESERVE,
</span></span><span style="display:flex;"><span>        PAGE_EXECUTE_READWRITE));
</span></span></code></pre></div><p>Si l’adresse préférée (<strong>ImageBase</strong>) est déjà occupée :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span>baseAlloc)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    baseAlloc <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>BYTE <span style="color:#f92672">*&gt;</span>(
</span></span><span style="display:flex;"><span>        VirtualAlloc(
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">nullptr</span>,
</span></span><span style="display:flex;"><span>            sizeOfImage,
</span></span><span style="display:flex;"><span>            MEM_COMMIT <span style="color:#f92672">|</span> MEM_RESERVE,
</span></span><span style="display:flex;"><span>            PAGE_EXECUTE_READWRITE));
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>On demande alors à <strong>VirtualAlloc</strong> d’allouer l’image à une adresse libre choisie automatiquement par Windows dans l’espace mémoire disponible du processus.</p>
<p>Cette allocation représente le comportement idéal du loader lorsqu’aucun conflit mémoire n’existe. En pratique, l’ASLR ou d’autres allocations peuvent forcer Windows à charger l’image à une adresse différente, nécessitant alors une étape de relocation.</p>
<h3 id="4-copie-des-headers">4. Copie des headers</h3>
<p>On copie la structure PE telle quelle en mémoire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>std<span style="color:#f92672">::</span>memcpy(baseAlloc, localCopy, sizeOfHeader);
</span></span></code></pre></div><h3 id="5-copie-des-sections">5. Copie des sections</h3>
<p>On récupère la table des sections :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>PIMAGE_SECTION_HEADER sectionHeaders <span style="color:#f92672">=</span> IMAGE_FIRST_SECTION(ntHeaders);
</span></span></code></pre></div><p>Puis on copie chaque section dans l’image allouée :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">for</span> (WORD i <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>; i <span style="color:#f92672">&lt;</span> ntHeaders<span style="color:#f92672">-&gt;</span>FileHeader.NumberOfSections; i<span style="color:#f92672">++</span>)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// dest (mémoire) -&gt; emplacement final en RAM
</span></span></span><span style="display:flex;"><span>    BYTE <span style="color:#f92672">*</span>dest <span style="color:#f92672">=</span> baseAlloc <span style="color:#f92672">+</span> sectionHeaders[i].VirtualAddress;
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">//src (fichier) -&gt; données brutes sur disque
</span></span></span><span style="display:flex;"><span>    BYTE <span style="color:#f92672">*</span>src  <span style="color:#f92672">=</span> localCopy <span style="color:#f92672">+</span> sectionHeaders[i].PointerToRawData; 
</span></span><span style="display:flex;"><span>    DWORD size <span style="color:#f92672">=</span> sectionHeaders[i].SizeOfRawData;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> (sectionHeaders[i].PointerToRawData <span style="color:#f92672">+</span> size <span style="color:#f92672">&gt;</span> rawSize)
</span></span><span style="display:flex;"><span>    {
</span></span><span style="display:flex;"><span>        std<span style="color:#f92672">::</span>cerr <span style="color:#f92672">&lt;&lt;</span> <span style="color:#e6db74">&#34;[!] Section &#34;</span> <span style="color:#f92672">&lt;&lt;</span> i <span style="color:#f92672">&lt;&lt;</span> <span style="color:#e6db74">&#34; dépasse la taille brute du payload.</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        VirtualFree(baseAlloc, <span style="color:#ae81ff">0</span>, MEM_RELEASE);
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">delete</span>[] localCopy;
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">nullptr</span>;
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    std<span style="color:#f92672">::</span>memcpy(dest, src, size);
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Dans une implémentation plus robuste, il faut également prendre en compte VirtualSize, car certaines sections sont plus grandes en mémoire que sur disque.</p>
<h2 id="relocations">Les Relocations : étape d’extension</h2>
<p>Les relocations ont déjà été présentées dans la partie théorie : elles servent à corriger les adresses internes du programme lorsque celui-ci n’est pas chargé à son <strong>ImageBase</strong> d’origine.</p>
<p>Dans une implémentation complète, cette étape repose sur la <strong>Base Relocation Table</strong>, qui contient des blocs d’adresses à ajuster en fonction du delta entre l’ImageBase souhaité et l’adresse réellement allouée en mémoire.</p>
<p>Dans notre cas, cette partie est volontairement laissée hors du périmètre de ce loader afin de garder une architecture simple et centrée sur les mécanismes principaux (mapping, imports, exécution).</p>
<blockquote>
<p>Cette étape constitue une extension naturelle pour un loader plus robuste et compatible avec davantage de binaires.</p>
</blockquote>
<h2 id="imports">Les Imports : Apprendre à parler avec les DLL système.</h2>
<p>Jusqu’ici, nous avons réussi à reconstruire l’image PE en mémoire et à copier ses sections. Mais un programme Windows ne vit jamais seul : il dépend presque toujours de fonctions externes fournies par les DLL système.</p>
<p>C’est exactement le rôle de la Import Table : dire au système &ldquo;j’ai besoin de ces fonctions-là pour fonctionner&rdquo;.</p>
<p>Notre job dans un loader manuel est donc simple en théorie, mais délicat en pratique :</p>
<p>Charger les DLL nécessaires, puis remplacer les pointeurs d’imports par les vraies adresses mémoire des fonctions.</p>
<h3 id="le-principe-des-imports-dans-un-pe">Le principe des imports dans un PE</h3>
<p>Quand un programme appelle par exemple :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>MessageBoxA(NULL, <span style="color:#e6db74">&#34;Hello&#34;</span>, <span style="color:#e6db74">&#34;Test&#34;</span>, <span style="color:#ae81ff">0</span>);
</span></span></code></pre></div><p>Il ne contient pas directement le code de MessageBoxA.
À la place, le compilateur laisse une &ldquo;place vide&rdquo; dans une table appelée :</p>
<ul>
<li><strong>IAT</strong> (<em>Import Address Table</em>) -&gt; adresses finales des fonctions</li>
<li><strong>INT</strong> (<em>Import Name Table</em>) -&gt; noms des fonctions utilisées</li>
</ul>
<h4 id="à-quoi-sert-lint-">À quoi sert l’INT ?</h4>
<p><strong>L’INT</strong> (<em>Import Name Table</em>) sert juste à dire :</p>
<blockquote>
<p>&ldquo;Voici les noms des fonctions que je veux utiliser&rdquo;</p>
</blockquote>
<p>Exemples :</p>
<ul>
<li>CreateFileA</li>
<li>MessageBoxA</li>
<li>VirtualAlloc</li>
</ul>
<h4 id="concrètement">Concrètement</h4>
<p>Quand Windows charge un programme :</p>
<ol>
<li>Il lit <strong>l’INT</strong></li>
<li>Il voit les noms des fonctions</li>
<li>Il va les chercher dans les DLL</li>
<li>Il remplit <strong>l’IAT</strong> avec leurs adresses</li>
</ol>
<p>Dans cette version pédagogique, nous nous limitons aux imports résolus par nom afin de garder une implémentation lisible. Les imports par ordinal, bien que présents dans certains binaires système, ne sont pas traités ici.</p>
<h3 id="lecture-de-la-import-table">Lecture de la Import Table</h3>
<p>Le point d’entrée de notre résolution d’imports commence ici :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_DATA_DIRECTORY <span style="color:#f92672">&amp;</span>importDir <span style="color:#f92672">=</span> ntHeaders<span style="color:#f92672">-&gt;</span>OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_IMPORT];
</span></span></code></pre></div><p>Cette structure nous donne :</p>
<blockquote>
<p>l’adresse et la taille de la table des imports dans le PE</p>
</blockquote>
<p>Si elle est vide :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span>importDir.VirtualAddress)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> true;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Cela signifie simplement :</p>
<blockquote>
<p>le programme n’a aucune dépendance externe -&gt; rien à faire</p>
</blockquote>
<h3 id="parcours-des-dll-importées">Parcours des DLL importées</h3>
<p>On récupère ensuite la première entrée :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_IMPORT_DESCRIPTOR <span style="color:#f92672">*</span>importDesc <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_IMPORT_DESCRIPTOR <span style="color:#f92672">*&gt;</span>(baseAddress <span style="color:#f92672">+</span> importDir.VirtualAddress);
</span></span></code></pre></div><p>Chaque <strong>IMAGE_IMPORT_DESCRIPTOR</strong> représente une DLL.
On boucle tant qu’il y a des DLL :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">while</span> (importDesc<span style="color:#f92672">-&gt;</span>Name)
</span></span></code></pre></div><h3 id="chargement-de-la-dll">Chargement de la DLL</h3>
<p>On récupère le nom de la DLL :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>LPCSTR dllName <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>LPCSTR<span style="color:#f92672">&gt;</span>(baseAddress <span style="color:#f92672">+</span> importDesc<span style="color:#f92672">-&gt;</span>Name);
</span></span></code></pre></div><p>Exemples possibles :</p>
<ul>
<li>kernel32.dll</li>
<li>user32.dll</li>
<li>ntdll.dll</li>
</ul>
<p>Puis on la charge en mémoire :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>HMODULE hMod <span style="color:#f92672">=</span> LoadLibraryA(dllName);
</span></span></code></pre></div><p>À ce stade :</p>
<ul>
<li>la DLL est mappée dans le processus</li>
<li>ses fonctions deviennent accessibles</li>
</ul>
<p>Si le chargement échoue :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span>hMod)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    std<span style="color:#f92672">::</span>cerr <span style="color:#f92672">&lt;&lt;</span> <span style="color:#e6db74">&#34;[!] Echec LoadLibraryA : &#34;</span> <span style="color:#f92672">&lt;&lt;</span> dllName <span style="color:#f92672">&lt;&lt;</span> std<span style="color:#f92672">::</span>endl;
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> false;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h3 id="les-deux-tables-critiques--originalfirstthunk-vs-firstthunk">Les deux tables critiques : OriginalFirstThunk vs FirstThunk</h3>
<p>C’est ici que les choses deviennent importantes.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_THUNK_DATA <span style="color:#f92672">*</span>thunkRef <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_THUNK_DATA <span style="color:#f92672">*&gt;</span>(baseAddress <span style="color:#f92672">+</span> importDesc<span style="color:#f92672">-&gt;</span>OriginalFirstThunk);
</span></span><span style="display:flex;"><span>IMAGE_THUNK_DATA <span style="color:#f92672">*</span>funcRef <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_THUNK_DATA <span style="color:#f92672">*&gt;</span>(baseAddress <span style="color:#f92672">+</span> importDesc<span style="color:#f92672">-&gt;</span>FirstThunk);
</span></span></code></pre></div><p><strong>OriginalFirstThunk</strong> (<em>INT</em>)</p>
<ul>
<li>Contient les noms des fonctions</li>
<li>Sert uniquement de référence</li>
</ul>
<p><strong>FirstThunk</strong> (<em>IAT</em>)</p>
<ul>
<li>Contient les adresses finales</li>
<li>C’est ce que le programme utilise réellement à l’exécution</li>
</ul>
<h4 id="cas-particulier">Cas particulier</h4>
<p>Certains binaires n’ont pas <strong>OriginalFirstThunk</strong> :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#f92672">!</span>importDesc<span style="color:#f92672">-&gt;</span>OriginalFirstThunk)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    thunkRef <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_THUNK_DATA <span style="color:#f92672">*&gt;</span>(baseAddress <span style="color:#f92672">+</span> importDesc<span style="color:#f92672">-&gt;</span>FirstThunk);
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Dans ce cas :</p>
<ul>
<li>la table des noms et la table des pointeurs sont confondues</li>
<li>solution alternative nécessaire</li>
</ul>
<h3 id="résolution-des-fonctions">Résolution des fonctions</h3>
<p>On parcourt chaque fonction importée :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">while</span> (thunkRef<span style="color:#f92672">-&gt;</span>u1.AddressOfData)
</span></span></code></pre></div><p>Pour chaque entrée :</p>
<h4 id="1-récupération-de-la-structure-import-by-name">1. Récupération de la structure &ldquo;import by name&rdquo;</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>IMAGE_IMPORT_BY_NAME <span style="color:#f92672">*</span>importByName <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>IMAGE_IMPORT_BY_NAME <span style="color:#f92672">*&gt;</span>(baseAddress <span style="color:#f92672">+</span> thunkRef<span style="color:#f92672">-&gt;</span>u1.AddressOfData);
</span></span></code></pre></div><p>Elle contient simplement :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>importByName<span style="color:#f92672">-&gt;</span>Name
</span></span></code></pre></div><p>Exemple :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#e6db74">&#34;CreateFileA&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;VirtualAlloc&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;MessageBoxA&#34;</span>
</span></span></code></pre></div><h4 id="2-résolution-de-ladresse-réelle">2. Résolution de l’adresse réelle</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>FARPROC <span style="color:#f92672">*</span>funcAddr <span style="color:#f92672">=</span> <span style="color:#66d9ef">reinterpret_cast</span><span style="color:#f92672">&lt;</span>FARPROC <span style="color:#f92672">*&gt;</span>(<span style="color:#f92672">&amp;</span>funcRef<span style="color:#f92672">-&gt;</span>u1.Function);
</span></span><span style="display:flex;"><span><span style="color:#f92672">*</span>funcAddr <span style="color:#f92672">=</span> GetProcAddress(hMod, importByName<span style="color:#f92672">-&gt;</span>Name);
</span></span></code></pre></div><p>Ici on fait le cœur du loader :</p>
<ul>
<li>on demande à Windows : où est cette fonction dans la DLL ?</li>
<li>GetProcAddress retourne son adresse mémoire réelle</li>
<li>on écrit cette adresse dans l’IAT</li>
</ul>
<h4 id="3-gestion-des-erreurs">3. Gestion des erreurs</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (<span style="color:#f92672">!*</span>funcAddr)
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    std<span style="color:#f92672">::</span>cerr <span style="color:#f92672">&lt;&lt;</span> <span style="color:#e6db74">&#34;[!] Impossible de résoudre l&#39;import : &#34;</span> <span style="color:#f92672">&lt;&lt;</span> dllName <span style="color:#f92672">&lt;&lt;</span> std<span style="color:#f92672">::</span>endl;
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> false;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Si une fonction est introuvable :</p>
<ul>
<li>DLL corrompue</li>
<li>version incompatible</li>
<li>export absent</li>
</ul>
<p>Le chargement échoue.</p>
<h4 id="4-avancement-des-pointeurs">4. Avancement des pointeurs</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>thunkRef<span style="color:#f92672">++</span>;
</span></span><span style="display:flex;"><span>funcRef<span style="color:#f92672">++</span>;
</span></span></code></pre></div><p>On passe simplement à l’entrée suivante.</p>
<h3 id="fin-de-traitement-dune-dll">Fin de traitement d’une DLL</h3>
<p>Quand toutes les fonctions d’une DLL sont résolues :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>importDesc<span style="color:#f92672">++</span>;
</span></span></code></pre></div><p>On passe à la DLL suivante.</p>
<h3 id="résultat-final-1">Résultat final</h3>
<p>Si tout s’est bien passé :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">return</span> true;
</span></span></code></pre></div><p>À ce stade :</p>
<ul>
<li>toutes les DLL sont chargées</li>
<li>toutes les fonctions sont résolues</li>
<li><strong>l’IAT</strong> est entièrement valide</li>
</ul>
<p>Le programme peut désormais s’exécuter comme s’il avait été lancé normalement par Windows.</p>
<h2 id="entrypoint">Le Point d’entrée : lancement du programme</h2>
<p>Une fois toutes les étapes précédentes terminées :</p>
<ul>
<li>image reconstruite en mémoire</li>
<li>sections copiées</li>
<li>imports résolus</li>
<li>relocations appliquées (si nécessaire)</li>
</ul>
<p>le programme est enfin prêt à s’exécuter.</p>
<p>Le point d’entrée est défini dans le header PE :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>AddressOfEntryPoint
</span></span></code></pre></div><p>Il correspond à un offset relatif à la base de l’image chargée en mémoire.</p>
<h3 id="ce-que-ça-signifie">Ce que ça signifie</h3>
<p>Contrairement à une idée simple, un programme Windows ne démarre pas directement sur <strong>main()</strong>.</p>
<p>Le flux réel est :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>EntryPoint <span style="color:#f92672">-&gt;</span> runtime C<span style="color:#f92672">/</span>C<span style="color:#f92672">++</span> <span style="color:#f92672">-&gt;</span> mainCRTStartup <span style="color:#f92672">-&gt;</span> main()
</span></span></code></pre></div><h4 id="rôle-du-loader">Rôle du loader</h4>
<p>Dans un loader manuel, le rôle final est simplement de :</p>
<blockquote>
<p>transférer l’exécution vers cet EntryPoint</p>
</blockquote>
<p>À partir de ce moment :</p>
<ul>
<li>le programme devient autonome</li>
<li>il s’exécute comme s’il avait été lancé normalement</li>
</ul>
<h4 id="résumé-du-pipeline-complet">Résumé du pipeline complet</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>Loader <span style="color:#f92672">-&gt;</span> reconstruction <span style="color:#f92672">-&gt;</span> imports <span style="color:#f92672">-&gt;</span> relocations <span style="color:#f92672">-&gt;</span> EntryPoint <span style="color:#f92672">-&gt;</span> programme
</span></span></code></pre></div><h2 id="anti-tampering">Anti-Tampering : Protéger notre code contre l&rsquo;analyse.</h2>
<p>Une fois le programme chargé, nous voulons le protéger contre toute modification en cours d’exécution. Pour cela, notre loader met en place une simple surveillance de l’intégrité mémoire via un <strong>CRC32</strong>. Le principe :</p>
<h3 id="calcul-du-crc-initial">Calcul du CRC initial</h3>
<p>Après avoir obtenu baseAlloc, on détermine <strong>sizeOfImage</strong> et on calcule un <strong>CRC32</strong> sur toute l’image :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">uint32_t</span> initialChecksum <span style="color:#f92672">=</span> crc32(baseAlloc, sizeOfImage);
</span></span></code></pre></div><p>La fonction <strong>crc32</strong> parcourt chaque octet et applique l’algorithme standard (polynôme <strong>0xEDB88320</strong>) :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">uint32_t</span> <span style="color:#a6e22e">crc32</span>(<span style="color:#66d9ef">const</span> <span style="color:#66d9ef">uint8_t</span><span style="color:#f92672">*</span> data, size_t size) {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">uint32_t</span> crc <span style="color:#f92672">=</span> <span style="color:#ae81ff">0xFFFFFFFF</span>;
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> (size_t i <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>; i <span style="color:#f92672">&lt;</span> size; i<span style="color:#f92672">++</span>) {
</span></span><span style="display:flex;"><span>        crc <span style="color:#f92672">^=</span> data[i];
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> (<span style="color:#66d9ef">int</span> j <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>; j <span style="color:#f92672">&lt;</span> <span style="color:#ae81ff">8</span>; j<span style="color:#f92672">++</span>) {
</span></span><span style="display:flex;"><span>            crc <span style="color:#f92672">=</span> (crc <span style="color:#f92672">&gt;&gt;</span> <span style="color:#ae81ff">1</span>) <span style="color:#f92672">^</span> (<span style="color:#ae81ff">0xEDB88320</span> <span style="color:#f92672">*</span> (crc <span style="color:#f92672">&amp;</span> <span style="color:#ae81ff">1</span>));
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#f92672">~</span>crc;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Ce calcul produit une empreinte « numérique » de l’image chargée.</p>
<h3 id="thread-de-surveillance">Thread de surveillance</h3>
<h4 id="pourquoi-crc32-">Pourquoi CRC32 ?</h4>
<p><strong>CRC32</strong> signifie :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>Cyclic Redundancy Check <span style="color:#ae81ff">32</span> bits
</span></span></code></pre></div><p>C’est un algorithme très rapide qui transforme un bloc mémoire en une valeur de <strong>32 bits</strong>.</p>
<p>Exemple :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>Zone mémoire A <span style="color:#f92672">-&gt;</span> <span style="color:#ae81ff">0x91AF22BC</span>
</span></span></code></pre></div><p>Si un seul octet change :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>Zone mémoire A modifiée <span style="color:#f92672">-&gt;</span> <span style="color:#ae81ff">0x4D880012</span>
</span></span></code></pre></div><p>Le résultat devient totalement différent.</p>
<p>Cela permet donc de détecter :</p>
<ul>
<li>patch mémoire</li>
<li>hook</li>
<li>injection</li>
<li>corruption</li>
<li>modification manuelle</li>
</ul>
<h5 id="pourquoi-pas-sha256-">Pourquoi pas SHA256 ?</h5>
<p>On pourrait utiliser un hash cryptographique moderne comme :</p>
<ul>
<li>SHA1</li>
<li>SHA256</li>
<li>BLAKE2</li>
</ul>
<p>Mais ici ce n’est pas nécessaire.</p>
<p>Notre objectif n’est pas de signer des données ni de résister à un attaquant cryptographique.</p>
<p>Nous voulons simplement :</p>
<ul>
<li>recalculer vite</li>
<li>comparer vite</li>
</ul>
<p><strong>CRC32</strong> est donc idéal car il est :</p>
<ul>
<li>beaucoup plus rapide</li>
<li>léger à implémenter</li>
<li>suffisant pour une surveillance mémoire simple</li>
</ul>
<blockquote>
<p>Cette approche est volontairement simple et sert uniquement à illustrer le concept de surveillance d’intégrité mémoire. Des implémentations réelles utilisent généralement des mécanismes plus complexes et distribués.</p>
</blockquote>
<h4 id="comment-fonctionne-notre-implémentation-">Comment fonctionne notre implémentation ?</h4>
<p>Au lancement du programme :</p>
<ol>
<li>Le loader calcule le <strong>CRC32</strong> de l’image PE chargée en mémoire</li>
<li>Il stocke cette valeur comme référence</li>
<li>Un thread secondaire tourne en arrière-plan</li>
<li>Toutes les quelques secondes, il recalcule le <strong>CRC32</strong></li>
<li>Si la valeur change : arrêt immédiat du processus</li>
</ol>
<p>C’est une forme simple d’auto-surveillance mémoire.</p>
<h5 id="implémentation">Implémentation</h5>
<p>On crée un thread détaché qui tourne en arrière-plan et recalcule périodiquement le <strong>CRC</strong> :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>std<span style="color:#f92672">::</span><span style="color:#66d9ef">thread</span>(AntiTamperThread, baseAlloc, sizeOfImage, initialChecksum).detach();
</span></span></code></pre></div><p>La fonction <strong>AntiTamperThread</strong> ressemble à :</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">void</span> <span style="color:#a6e22e">AntiTamperThread</span>(BYTE<span style="color:#f92672">*</span> baseAddress, SIZE_T sizeOfImage, <span style="color:#66d9ef">uint32_t</span> initialChecksum) {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">while</span> (true) {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">uint32_t</span> currentChecksum <span style="color:#f92672">=</span> crc32(baseAddress, sizeOfImage);
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> (currentChecksum <span style="color:#f92672">!=</span> initialChecksum) {
</span></span><span style="display:flex;"><span>            std<span style="color:#f92672">::</span>cerr <span style="color:#f92672">&lt;&lt;</span> <span style="color:#e6db74">&#34;Modification détecte</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">&#34;</span>;
</span></span><span style="display:flex;"><span>            ExitProcess(<span style="color:#ae81ff">0</span>);
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>        std<span style="color:#f92672">::</span>this_thread<span style="color:#f92672">::</span>sleep_for(std<span style="color:#f92672">::</span>chrono<span style="color:#f92672">::</span>seconds(<span style="color:#ae81ff">3</span>));
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><h5 id="explication-ligne-par-ligne">Explication ligne par ligne</h5>
<p><strong>Boucle infinie</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">while</span> (true)
</span></span></code></pre></div><p>Le thread surveille continuellement la mémoire tant que le programme tourne.</p>
<p><strong>Recalcul du checksum</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">uint32_t</span> currentChecksum <span style="color:#f92672">=</span> crc32(baseAddress, sizeOfImage);
</span></span></code></pre></div><p>On relit toute l’image PE chargée en mémoire :</p>
<ul>
<li>code .text</li>
<li>données</li>
<li>imports patchés</li>
<li>sections copiées</li>
</ul>
<p>Puis on calcule une nouvelle empreinte <strong>CRC32</strong>.</p>
<p><strong>Comparaison</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span><span style="color:#66d9ef">if</span> (currentChecksum <span style="color:#f92672">!=</span> initialChecksum)
</span></span></code></pre></div><p>Si la nouvelle valeur diffère de l’originale :</p>
<blockquote>
<p>quelque chose a changé en mémoire.</p>
</blockquote>
<p><strong>Réaction</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>ExitProcess(<span style="color:#ae81ff">0</span>);
</span></span></code></pre></div><p>Le programme se ferme immédiatement.</p>
<p><strong>Pause entre deux scans</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-cpp" data-lang="cpp"><span style="display:flex;"><span>sleep_for(<span style="color:#ae81ff">3</span> secondes)
</span></span></code></pre></div><p>Cela évite de monopoliser le <strong>CPU</strong>.</p>
<p>Le contrôle est périodique, léger, discret.</p>
<h5 id="limites-de-cette-approche">Limites de cette approche</h5>
<p>Cette protection reste volontairement simple.</p>
<p>Un analyste expérimenté pourrait :</p>
<ul>
<li>patcher la fonction de vérification elle-même (NOP du check ou saut conditionnel forcé)</li>
<li>hooker la fonction <strong>crc32</strong> pour retourner toujours la même valeur</li>
<li>suspendre ou neutraliser le thread de surveillance</li>
<li>recalculer une nouvelle somme de référence après modification légitime</li>
<li>debugger le processus et modifier la mémoire avant la vérification</li>
<li>hooker <strong>ExitProcess</strong></li>
</ul>
<p>En pratique, l’objectif n’est pas d’empêcher totalement la modification, mais d’augmenter suffisamment le coût et la complexité de l’analyse pour décourager les attaques automatisées ou rapides.</p>
<p>C’est pour cette raison que les protections modernes ne reposent jamais sur un seul checksum isolé comme celui-ci.</p>
<h5 id="idées-damélioration">Idées d’amélioration</h5>
<p>Pour aller plus loin :</p>
<ul>
<li>surveiller uniquement .text</li>
<li>utiliser plusieurs checksums</li>
<li>lancer plusieurs threads</li>
<li>vérifier à intervalles aléatoires</li>
<li>conserver certaines zones sensibles chiffrées en mémoire et ne les déchiffrer qu’au runtime (uniquement lors de leur utilisation)</li>
<li>combiner avec anti-debug</li>
</ul>
<p>Mais la base reste toujours la même :</p>
<blockquote>
<p>Détecter qu’un tiers a modifié ce qui devait rester intact.</p>
</blockquote>
<h2 id="conclusion">Conclusion</h2>
<p>Dans ce projet, nous avons construit pas à pas les fondations d’un chargeur PE minimaliste, en reproduisant manuellement une grande partie du comportement du loader de Windows.</p>
<p>Même si certaines étapes comme les relocations complètes ou des mécanismes avancés de robustesse n’ont pas été implémentées ici, l’architecture générale d’un manual mapper fonctionnel est désormais en place.</p>
<p>Les techniques présentées ici constituent les bases du fonctionnement interne des loaders Windows. Elles sont reprises et étendues dans des loaders plus avancés, ainsi que dans des outils d’analyse et de reverse engineering utilisés en sécurité informatique.</p>
<p>Comprendre ces mécanismes permet donc à la fois de mieux appréhender le fonctionnement interne de Windows, mais aussi d’analyser et de détecter des comportements avancés dans des contextes de sécurité.</p>
<p>Le code complet du projet est disponible ici :
<a href="https://github.com/n0lanndev/pe-loader-toolkit">https://github.com/n0lanndev/pe-loader-toolkit</a></p>
<p>nolanndev</p>
<h2 id="disclaimer">⚠️ Avertissement Légal (Disclaimer)</h2>
<p>Ce cours est partagé à des fins strictement éducatives et dans un but de recherche en cybersécurité. Les techniques présentées (packing, chiffrement, manipulation de binaires) servent à comprendre le fonctionnement interne des systèmes Windows et des mécanismes de défense.</p>
<ul>
<li><strong>Usage responsable</strong> : Ne testez jamais ces concepts sur un ordinateur ou un réseau qui ne vous appartient pas.</li>
<li><strong>Responsabilité</strong> : L&rsquo;auteur décline toute responsabilité en cas de mauvaise utilisation de ces informations ou de dommages causés par une utilisation en dehors d&rsquo;un cadre légal et éthique.</li>
<li><strong>Éthique</strong> : Le savoir est une arme ; utilisez-la pour construire des systèmes plus sûrs, pas pour nuire.</li>
</ul>
]]></content>
        </item>
        
    </channel>
</rss>
